const fileInput=document.getElementById("file"),scanButton=document.getElementById("scan"),result=document.getElementById("result");
function escapeHtml(v){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));}
scanButton.addEventListener("click",async()=>{result.classList.remove("hidden");if(!fileInput.files.length){result.innerHTML="<h2>Select a file first.</h2>";return;}const form=new FormData();form.append("file",fileInput.files[0]);scanButton.disabled=true;result.innerHTML="<h2>Scanning…</h2>";try{const response=await fetch("/api/scan",{method:"POST",body:form});const data=await response.json();if(!response.ok)throw new Error(data.detail||"Scan failed");const status=data.safe_to_share?"No high-risk findings":"Do not share yet";const rows=data.findings.map(f=>`<tr><td>${escapeHtml(f.severity)}</td><td>${escapeHtml(f.category)}</td><td>${f.line}:${f.column}</td><td>${escapeHtml(f.match_preview)}</td><td>${escapeHtml(f.recommendation)}</td></tr>`).join("");result.innerHTML=`<h2>${escapeHtml(status)}</h2><p><strong>${escapeHtml(data.filename)}</strong> — ${data.bytes_scanned} bytes scanned, ${data.summary.total} finding(s).</p>${rows?`<div class="table-wrap"><table><thead><tr><th>Severity</th><th>Type</th><th>Location</th><th>Preview</th><th>Recommendation</th></tr></thead><tbody>${rows}</tbody></table></div>`:"<p>Nothing matched the current detection rules.</p>"}`;}catch(error){result.innerHTML=`<h2>Scan error</h2><p>${escapeHtml(error.message)}</p>`}finally{scanButton.disabled=false;}});
